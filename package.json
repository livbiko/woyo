{
  "name": "woyo",
  "private": true,
  "description": "Woyo -- marketing site for the Woyo ride-share app (Cote d'Ivoire). apps/web is a standalone Next.js project (its own package.json/lockfile) -- run `npm install && npm run dev` from inside apps/web directly."
}

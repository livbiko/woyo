# In-cluster image build, no local Docker needed. Context is the repo ROOT
# (not apps/api) because the Dockerfile needs packages/shared alongside apps/api.
apiVersion: batch/v1
kind: Job
metadata:
  name: kaniko-build-woyo-api
  namespace: woyo
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: kaniko
          image: gcr.io/kaniko-project/executor:latest
          args:
            - "--context=git://github.com/livbiko/woyo.git#refs/heads/master"
            - "--dockerfile=apps/api/Dockerfile"
            - "--destination=lhr.ocir.io/lr14abpkfrxj/woyo-api:latest"
          volumeMounts:
            - name: docker-config
              mountPath: /kaniko/.docker
      volumes:
        - name: docker-config
          secret:
            secretName: ocir-pull-secret
            items:
              - key: .dockerconfigjson
                path: config.json

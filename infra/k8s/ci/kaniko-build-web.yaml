# apps/web is standalone (own lockfile, not a workspace member), so its
# Kaniko context is scoped to that subdirectory via --context-sub-path,
# same pattern as NouvellesDuPays's web build. No build-args needed --
# this is a static marketing site, no backend/API to point at.
apiVersion: batch/v1
kind: Job
metadata:
  name: kaniko-build-woyo-web
  namespace: woyo
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
        - name: kaniko
          image: gcr.io/kaniko-project/executor:latest
          args:
            - "--context=git://github.com/livbiko/woyo.git#refs/heads/master"
            - "--context-sub-path=apps/web"
            - "--dockerfile=Dockerfile"
            - "--destination=lhr.ocir.io/lr14abpkfrxj/woyo-web:latest"
          volumeMounts:
            - name: docker-config
              mountPath: /kaniko/.docker
      volumes:
        - name: docker-config
          secret:
            secretName: ocir-pull-secret
            items:
              - key: .dockerconfigjson
                path: config.json

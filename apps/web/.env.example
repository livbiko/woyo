NEXT_PUBLIC_API_URL="http://localhost:4001/api"
# Only needed when running behind Docker Compose, where Server Components
# inside this container must reach the api service by its Docker network
# name rather than localhost. Leave unset for plain `npm run dev`.
API_URL=

